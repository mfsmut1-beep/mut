<!-- simpan file ini dengan nama: selamat-ulang-tahun-bang-nando-sayang.html -->
<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Selamat Ulang Tahun Bang Nando Sayang 🎂</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      font-family: 'Comic Sans MS', cursive, sans-serif;
      overflow: hidden;
    }

    .slide {
      background: white;
      padding: 40px;
      border-radius: 20px;
      text-align: center;
      box-shadow: 0 10px 25px rgba(0,0,0,0.2);
      display: none;
      animation: pop 0.6s ease forwards;
      max-width: 500px;
    }

    .slide h1 {
      font-size: 2rem;
      color: #ff4081;
      margin-bottom: 15px;
    }

    .slide p {
      font-size: 1.1rem;
      color: #444;
      margin-bottom: 12px;
      line-height: 1.5em;
    }

    .slide img {
      width: 200px;
      height: 200px;
      object-fit: cover;
      border-radius: 50%;
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
      margin-bottom: 20px;
    }

    .btn {
      padding: 15px 35px;
      border: none;
      border-radius: 50px;
      background: #ff6b81;
      color: white;
      font-size: 1.2rem;
      cursor: pointer;
      box-shadow: 0 5px 15px rgba(0,0,0,0.3);
      position: absolute;
      transition: transform 0.2s, background 0.2s;
    }

    .btn:hover {
      background: #ff4757;
      transform: scale(1.1);
    }

    .nextBtn {
      margin-top: 20px;
      padding: 12px 28px;
      border: none;
      border-radius: 30px;
      background: #ff4d6d;
      color: white;
      cursor: pointer;
      font-size: 1rem;
      transition: 0.2s;
    }

    .nextBtn:hover {
      transform: scale(1.1);
      background: #ff1e56;
    }

    .closing {
      margin-top: 25px;
    }

    .closing button {
      margin: 10px;
      padding: 12px 25px;
      border: none;
      border-radius: 30px;
      background: #ff4d6d;
      color: white;
      cursor: pointer;
      font-size: 1rem;
      transition: 0.2s;
      position: relative;
    }

    .closing button::before {
      content: "❤️ ";
      font-size: 1.2rem;
    }

    .closing button:hover {
      transform: scale(1.1);
      background: #ff1e56;
    }

    #closingMessage {
      margin-top: 20px;
      font-size: 1.3rem;
      font-weight: bold;
      color: #ff4081;
    }

    @keyframes pop {
      0% { transform: scale(0.5); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }

    canvas {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
    }
  </style>
</head>
<body>
  <button class="btn" id="openBtn">🎁 ❤️ Buka Ucapan ❤️</button>

  <!-- Slide 1 -->
  <div class="slide" id="slide1">
    <img src="FOTO_KAMU.jpg" alt="Foto Ulang Tahun">
    <h1>🎉 Selamat Ulang Tahun Bang Nando Sayang 🎂</h1>
    <p>Hari ini adalah hari spesial karena Bang Nando tambah usia... dan tambah manis 😘</p>
    <button class="nextBtn" onclick="nextSlide(2)">➡️ ❤️ Selanjutnya ❤️</button>
  </div>

  <!-- Slide 2 -->
  <div class="slide" id="slide2">
    <h1>✨ Doa Terbaik ✨</h1>
    <p>Semoga panjang umur, sehat selalu, rezeki lancar, dan semua impian Bang Nando tercapai 🎯</p>
    <p>Dan semoga wajah makin glowing tanpa perlu skincare mahal 🤭</p>
    <button class="nextBtn" onclick="nextSlide(3)">➡️ ❤️ Selanjutnya ❤️</button>
  </div>

  <!-- Slide 3 -->
  <div class="slide" id="slide3">
    <h1>😆 Sedikit Bercanda</h1>
    <p>Ulang tahun tanpa traktiran itu kayak mie instan tanpa micin... nggak ada gregetnya 🍜😂</p>
    <p>Jadi... jangan lupa traktir yaaa 🎂🍕🥤</p>

    <div class="closing">
      <p>Pilih jawabanmu 😏👇</p>
      <button onclick="showClosing(1)">Makasih cantik 😘</button>
      <button onclick="showClosing(2)">Makasih cantik manis sayangku 💖</button>
      <button onclick="showClosing(3)">Makasih ya 😐</button>
      <div id="closingMessage"></div>
    </div>
  </div>

  <canvas id="confetti"></canvas>

  <audio id="music" src="https://www2.cs.uic.edu/~i101/SoundFiles/ImperialMarch60.wav"></audio>

  <script>
    const btn = document.getElementById("openBtn");
    const music = document.getElementById("music");
    const closingMessage = document.getElementById("closingMessage");

    // Tombol nakal
    btn.addEventListener("mouseover", () => {
      const x = Math.random() * (window.innerWidth - btn.offsetWidth);
      const y = Math.random() * (window.innerHeight - btn.offsetHeight);
      btn.style.left = `${x}px`;
      btn.style.top = `${y}px`;
    });

    // Klik tombol buka ucapan
    btn.addEventListener("click", () => {
      btn.style.display = "none";
      document.getElementById("slide1").style.display = "block";
      startConfetti();
      music.play();
    });

    // Fungsi next slide
    function nextSlide(num) {
      document.querySelectorAll(".slide").forEach(s => s.style.display = "none");
      document.getElementById("slide" + num).style.display = "block";
      startConfetti();
    }

    // Fungsi closing
    function showClosing(choice) {
      if (choice === 1) {
        closingMessage.innerHTML = "Hehe iya sama-sama cantik ❤️ jangan lupa traktir 😘🎂";
      } else if (choice === 2) {
        closingMessage.innerHTML = "Aduh sweet banget ❤️ aku jadi melting 🥰";
      } else {
        closingMessage.innerHTML = "Ih kok cuek amat 😐... yaudah deh ❤️😆";
      }
      startConfetti();
    }

    // Konfeti 🎉
    const canvas = document.getElementById("confetti");
    const ctx = canvas.getContext("2d");
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;

    const pieces = [];
    const numPieces = 100;

    function ConfettiPiece() {
      this.x = Math.random() * canvas.width;
      this.y = Math.random() * canvas.height - canvas.height;
      this.size = Math.random() * 10 + 5;
      this.speed = Math.random() * 3 + 2;
      this.color = `hsl(${Math.random() * 360}, 100%, 50%)`;
      this.rotation = Math.random() * 360;
    }

    ConfettiPiece.prototype.update = function() {
      this.y += this.speed;
      if (this.y > canvas.height) {
        this.y = -10;
        this.x = Math.random() * canvas.width;
      }
    };

    ConfettiPiece.prototype.draw = function() {
      ctx.fillStyle = this.color;
      ctx.save();
      ctx.translate(this.x, this.y);
      ctx.rotate(this.rotation * Math.PI / 180);
      ctx.fillRect(-this.size/2, -this.size/2, this.size, this.size);
      ctx.restore();
    };

    function startConfetti() {
      for (let i = 0; i < numPieces; i++) {
        pieces.push(new ConfettiPiece());
      }
      requestAnimationFrame(updateConfetti);
    }

    function updateConfetti() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      pieces.forEach(p => {
        p.update();
        p.draw();
      });
      requestAnimationFrame(updateConfetti);
    }

    window.addEventListener("resize", () => {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    });
  </script>
</body>
</html>
